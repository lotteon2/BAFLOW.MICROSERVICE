server:
  port: 8084
spring:
  application:
    name: onedayclass-service
  config:
    activate:
      on-profile: local, dev, local
    import:
      - "optional:configserver:http://config-service:8888"
      - "optional:configserver:http://localhost:8888"
management:
  endpoints:
    web:
      exposure:
        include:
          - "refresh"
          - "bus-refresh"